# URL pública del backend Django (incluye /api). En Netlify se define en
# Site settings → Environment variables.
VITE_API_URL=https://tu-backend.onrender.com/api

# Usar datos de demo (mock) en vez del backend real. En producción: false.
VITE_USE_MOCK=false
